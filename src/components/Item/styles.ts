import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
  },
  description: {
    flex: 1,
    fontSize: 14,
    fontWeight: 700,
  },
});
